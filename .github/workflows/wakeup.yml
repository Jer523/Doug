name: Keep Douglas Awake

on:
  schedule:
    - cron: '0 4,10,16,22 * * *'
  workflow_dispatch:

jobs:
  wake-up:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Douglas
        run: |
          echo "正在访问 Douglas..."
          curl -s --max-redirs 20 -L https://douglas48.streamlit.app/ > /dev/null || true
          echo "完成"
